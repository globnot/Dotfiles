# Thème Qt (qt5ct/qt6ct + palette Gruvbrutal) pour les rares apps Qt sur ce
# bureau sinon tout GTK — surtout polkit-kde-agent (popup d'authentification
# root). Mirroir de ce que hyprland.lua fixe via hl.env() pour les process
# qu'Hyprland lance lui-même ; ce fichier couvre en plus les services
# activés par systemd/D-Bus (uwsm), qui n'héritent pas forcément de l'env
# d'Hyprland.
QT_QPA_PLATFORMTHEME=qt6ct
