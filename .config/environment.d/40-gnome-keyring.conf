# Agent SSH fourni par gnome-keyring (voir /etc/pam.d/sddm, déjà configuré
# pour démarrer/déverrouiller le trousseau à la connexion). Le composant
# ssh de gnome-keyring-daemon crée toujours son socket à ce chemin.
SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/keyring/ssh
